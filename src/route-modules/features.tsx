import Features from "@/pages/Features";
import { createPageLinks, createPageMeta } from "./shared";

export const meta = () => createPageMeta("features");

export const links = () => createPageLinks("features");

export default Features;
