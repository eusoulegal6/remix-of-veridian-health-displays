import Pricing from "@/pages/Pricing";
import { createPageLinks, createPageMeta } from "./shared";

export const meta = () => createPageMeta("pricing");

export const links = () => createPageLinks("pricing");

export default Pricing;
