import Clinics from "@/pages/Clinics";
import { createPageLinks, createPageMeta } from "./shared";

export const meta = () => createPageMeta("clinics");

export const links = () => createPageLinks("clinics");

export default Clinics;
