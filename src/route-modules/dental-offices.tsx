import DentalOffices from "@/pages/DentalOffices";
import { createPageLinks, createPageMeta } from "./shared";

export const meta = () => createPageMeta("dentalOffices");

export const links = () => createPageLinks("dentalOffices");

export default DentalOffices;
