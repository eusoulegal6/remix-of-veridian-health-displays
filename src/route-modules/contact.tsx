import Contact from "@/pages/Contact";
import { createPageLinks, createPageMeta } from "./shared";

export const meta = () => createPageMeta("contact");

export const links = () => createPageLinks("contact");

export default Contact;
