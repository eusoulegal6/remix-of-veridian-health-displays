import FAQ from "@/pages/FAQ";
import { createPageLinks, createPageMeta } from "./shared";

export const meta = () => createPageMeta("faq");

export const links = () => createPageLinks("faq");

export default FAQ;
